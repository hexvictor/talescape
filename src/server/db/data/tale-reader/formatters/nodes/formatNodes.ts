/**
 * Re-exports the production node formatter from the new reader formatter set.
 *
 * @example
 * import { formatNodes } from "../nodes/formatNodes";
 */
export { formatNodes } from "../tale/formatNodes";
