/**
 * Re-exports the production part formatter from the new reader formatter set.
 *
 * @example
 * import { formatParts } from "../parts/formatParts";
 */
export { formatParts } from "../tale/formatParts";
