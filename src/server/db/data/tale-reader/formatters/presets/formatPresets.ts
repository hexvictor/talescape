/**
 * Re-exports production preset formatters from the new reader formatter set.
 *
 * @example
 * import { formatAnimationPresets } from "../presets/formatPresets";
 */
export {
	formatAnimationPresets,
	formatStylePresets,
	formatTransitionPresets,
	formatVisibilityPresets,
} from "../tale/formatPresets";
