import { LibraryDetailPlaceholder } from "../../../_components/LibraryContent";

export default function TaleOverview() {
	return (
		<LibraryDetailPlaceholder
			label="Tale"
			title="Tale overview"
			description="A reading path preview with progress, connected fragments, and codex context."
		/>
	);
}
