import { LibraryDetailPlaceholder } from "../../../_components/LibraryContent";

export default function StoryFragmentOverview() {
	return (
		<LibraryDetailPlaceholder
			label="Fragment"
			title="Story fragment"
			description="A saved scene, letter, song, or stray discovery that can later connect into a tale."
		/>
	);
}
